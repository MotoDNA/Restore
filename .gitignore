.DS_Store
_seed-*.js
supabase/.temp/

# ⚠ 이 저장소는 공개입니다 (무료 계정은 깃허브 페이지를 쓰려면 공개여야 합니다).
#   거래처·가맹점의 **실제 단가표를 여기 커밋하지 마세요.** 예전에 실제 고객 발주가
#   공개 저장소로 새어 나간 적이 있습니다.
#   실제 단가는 git 이 아니라 **앱의 품목 탭**(데이터베이스)에만 둡니다.
#   받아 온 단가 파일은 아래 이름으로 두면 실수로도 안 올라갑니다.
sql/_real-*.sql
*단가*.xlsx
*단가*.csv
