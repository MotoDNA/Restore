# Re:Store 설치 — 20분

Re:Call · Re:Bind 가 이미 깔려 있다는 전제입니다.
데이터베이스도, 계정도 그것을 그대로 씁니다. 새로 할 일은 넷입니다.

```
Project ref : izrtclsqhsgkuwsffifn
Project URL : https://izrtclsqhsgkuwsffifn.supabase.co
```

---

## 1. 표 만들기 (한 번)

[SQL 편집기](https://supabase.com/dashboard/project/izrtclsqhsgkuwsffifn/sql/new)에
`sql/0001_restore.sql` 을 통째로 붙여넣고 실행합니다.

생기는 것:

| | |
|---|---|
| `stores` | 가맹점 하나 = 한 줄 (발주 링크와 핀도 여기 있습니다) |
| `supply_items` | 본사가 파는 품목과 단가 |
| `orders` | 발주서 한 장 |
| `order_lines` | 발주 줄 (품목명·단가를 그때 값으로 박아 둡니다) |
| `supply` 보관함 | 품목 사진. 경로가 `{회사id}/{품목id}/…` 라 폴더 이름이 곧 권한입니다 |

Re:Call · Re:Bind 의 표는 건드리지 않습니다. 실행해도 기존 자료는 그대로입니다.

> `supabase db push` 는 쓰지 마세요. 이 폴더에는 앞 번호 파일이 없어서 CLI 가 헷갈립니다.
> 그래서 `migrations` 가 아니라 `sql` 폴더에 두었습니다. 붙여넣기로 한 번만 실행합니다.

## 2. 구도로 통닭 만들기

같은 SQL 편집기에 `sql/0002_gudoro.sql` 을 붙여넣고 실행합니다.
회사(`GUDORO`)가 생기고, **`apps` 에 `restore` 가 들어갑니다.**

> ⚠ `apps` 에 `restore` 가 없으면 데이터베이스가 표를 통째로 닫습니다.
> 로그인은 되는데 아무것도 안 보이면 이것부터 확인하세요.
> 회사를 새로 만들 때마다 `apps` 를 함께 넣어야 합니다.

> ⚠ 그 파일에 든 **품목과 단가는 제가 지어낸 예시**입니다.
> 실제 것을 받아 와서 고쳐야 합니다. 틀린 단가로 정산서가 나가면 사고입니다.

## 3. 관리자 계정 만들기

Re:Call 의 `signup` 함수가 만듭니다. Re:Call 저장소의 `백엔드-설치방법.md`
"회사·관리자 만들기" 절차를 그대로 따르되, 회사 코드만 `GUDORO` 로 씁니다.
**비밀번호는 사장님이 직접 정해서 넣습니다.**

## 4. 서버 함수 배포 (한 번)

점주는 계정이 없습니다. 링크의 열쇠와 핀을 확인하고 **그 점포 것만** 읽고 써 주는
창구를 따로 둡니다. 데이터베이스는 로그인하지 않은 사람에게 아무 권한도 주지 않습니다.

```bash
supabase functions deploy store-gate --no-verify-jwt --project-ref izrtclsqhsgkuwsffifn
```

`--no-verify-jwt` 를 빠뜨리면 점주가 링크를 열 수 없습니다. 꼭 넣으세요.

### ⚠ 여기서 다른 두 앱이 함께 멈출 수 있습니다

`ALLOWED_ORIGIN` 은 **세 앱이 함께 쓰는 값 하나**입니다.
Re:Store 주소만 적어 넣으면 그 순간 Re:Bind 와 Re:Call 이 멈춥니다.
**늘 세 주소를 함께** 적으세요.

```bash
supabase secrets set ALLOWED_ORIGIN="https://restore.dnalabs.kr,https://rebind.dnalabs.kr,https://recall.dnalabs.kr" \
  --project-ref izrtclsqhsgkuwsffifn
```

값을 바꾼 뒤에는 **다섯 함수를 모두 다시 배포해야** 반영됩니다.

```bash
supabase functions deploy store-gate --no-verify-jwt --project-ref izrtclsqhsgkuwsffifn
supabase functions deploy share-view --no-verify-jwt --project-ref izrtclsqhsgkuwsffifn   # Re:Bind
supabase functions deploy read-order --project-ref izrtclsqhsgkuwsffifn                   # Re:Bind
supabase functions deploy read-card  --project-ref izrtclsqhsgkuwsffifn                   # Re:Call
supabase functions deploy admin-user --project-ref izrtclsqhsgkuwsffifn                   # Re:Call
```

## 5. 앱 올리기

`store.html` 과 `index.html` 을 깃허브 페이지에 올리고, `CNAME` 대로
`restore.dnalabs.kr` 을 연결합니다. Re:Bind 와 같은 방식입니다.

---

## 첫 실행 — 이 순서대로 하세요

1. `restore.dnalabs.kr` 을 열고 **GUDORO** 회사 코드로 들어갑니다
2. **설정 → 우리 회사** 에 사업자등록번호·대표자·주소·업태·종목·계좌를 넣습니다
   (관리자만 고칩니다. 여기 넣은 것이 명세서 오른쪽 "공급자" 칸에 찍힙니다)
3. **품목** 탭에서 실제 품목과 단가로 고칩니다. 면세/과세를 꼭 확인하세요
4. **가맹점 → 점포 추가** 로 점포를 하나 넣습니다
   (링크와 핀이 자동으로 만들어집니다)
5. 그 점포를 열고 **문자로 보내기** 를 눌러 점주에게 링크와 핀을 보냅니다
6. 다른 브라우저(시크릿 창)에서 그 링크를 열어 **핀을 넣고 발주해 봅니다**
7. 본사 화면으로 돌아와 **확정 → 출고 → 완료** 를 눌러 봅니다
8. **정산** 탭에서 그 달을 골라 명세서가 나오는지 봅니다

## 로컬에서 볼 때

```bash
cd /Users/motodna/Desktop/Restore && python3 -m http.server 8791
```

`http://localhost:8791/store.html` 로 엽니다.
점주 화면은 `http://localhost:8791/store.html?t=<토큰>` 입니다.

---

## 잘 안 될 때

| 증상 | 볼 곳 |
|---|---|
| 로그인은 되는데 아무것도 안 보임 | 회사의 `apps` 에 `restore` 가 들었는지 (2번) |
| "Re:Store 를 쓰는 회사가 아닙니다" | 같은 원인입니다. 2번 |
| 점주 링크가 "없는 주소입니다" | 4번 배포에 `--no-verify-jwt` 가 빠졌는지 · `ALLOWED_ORIGIN` 에 그 주소가 있는지 · 점포의 링크가 켜져 있는지 |
| 점주가 "핀이 다릅니다" | 가맹점 화면의 핀과 알려 준 핀이 같은지. 다르면 **핀 새로** 를 눌러 다시 보냅니다 |
| 갑자기 Re:Bind·Re:Call 이 멈춤 | `ALLOWED_ORIGIN` 에 세 주소를 다 적었는지 (4번) |
| 품목 사진이 안 올라감 | 1번 SQL 의 `supply` 보관함이 만들어졌는지 |
| 명세서 공급자 칸이 비어 있음 | 설정 → 우리 회사를 관리자 계정으로 저장했는지 |

[함수 로그](https://supabase.com/dashboard/project/izrtclsqhsgkuwsffifn/functions)와
브라우저 콘솔을 함께 보면 원인이 나옵니다.
