# Re:Store — 진행 상황

가맹점 발주와 정산 · DNA Labs

마지막 정리: 2026-08-30 (만들고 설치까지)

## 링크

| | |
|---|---|
| **앱** | https://restore.dnalabs.kr/ — 올렸습니다. **DNS 한 줄만 넣으면 열립니다** |
| **저장소** | https://github.com/MotoDNA/Restore (공개) |
| Re:Bind | https://rebind.dnalabs.kr/ |
| Re:Call | https://recall.dnalabs.kr/ |
| **Supabase 프로젝트** | https://supabase.com/dashboard/project/izrtclsqhsgkuwsffifn |
| **SQL 편집기** | https://supabase.com/dashboard/project/izrtclsqhsgkuwsffifn/sql/new |
| **함수 로그** | https://supabase.com/dashboard/project/izrtclsqhsgkuwsffifn/functions |

**첫 고객: 구도로 통닭** (친구분이 일하는 회사, 가맹사업).
회사 코드는 `GUDORO` 로 잡아 두었습니다.

---

## 무엇을 만들었나

가맹점이 **링크만 열어** 발주하고, 본사는 들어온 것을 한 화면에서 확정·출고합니다.
한 달이 지나면 쌓인 숫자가 **그대로 정산서**가 됩니다.

Re:Call · Re:Bind 와 **같은 Supabase 프로젝트, 같은 계정**입니다.

```
Re:Call   network-dna.html   고객 · 명함 · 미팅        (~/Desktop/network-dna)
Re:Bind   bindery.html       프로젝트 · 공정 · 명세서   (~/Desktop/Rebind)
Re:Store  store.html         가맹점 · 발주 · 월정산     (~/Desktop/Restore)
              └── 같은 데이터베이스 · 같은 로그인
```

---

## 지금 상태

### 다 된 것 — 코드

- **표 4개와 접근 규칙** — `sql/0001_restore.sql` (386줄)
  - `stores` 가맹점 (발주 링크 토큰 · 핀 · 사업자 정보 · 담당 슈퍼바이저)
  - `supply_items` 본사가 파는 품목 (단가 · 면세여부 · 최소발주수량 · 박스당 수량)
  - `orders` 발주서 한 장 (접수 → 확정 → 출고 → 완료 · 송장 · 정산 마감표시)
  - `order_lines` 발주 줄 (**품목명·규격·단가를 그때 값으로 박아 둡니다**)
  - `supply` 보관함 — 품목 사진. 경로가 `{회사id}/{품목id}/…` 라 폴더 이름이 곧 권한
  - 규칙은 모두 `company_for_app('restore')` 를 지납니다. 안 산 회사는 표가 통째로 닫힙니다
  - `company_settings`(명세서 공급자 정보)는 **Re:Bind 것을 그대로 씁니다.** 새로 안 만들었습니다
- **회사 열어 주기 + 품목** — `sql/0002_company.sql` (apps 에 restore 넣기 + 품목 12개 예시)
- **서버 함수 `store-gate`** — 토큰과 핀을 확인하고 그 점포 것만 읽고 씁니다 (249줄)
  - 단가는 브라우저에서 안 받습니다. 서버가 품목표에서 직접 꺼내 붙입니다
  - 최소 발주 수량을 서버에서도 한 번 더 봅니다
  - 핀 틀린 횟수를 점포별로 셉니다 (10번 틀리면 10분 잠금)
  - 구독이 끊긴 회사(`apps` 에 restore 없음)면 점주 링크도 함께 닫힙니다
  - 내부 메모(`memo_hq`) · 다른 점포 · 담당자 id 는 나가지 않습니다
- **앱 `store.html`** — 파일 하나 (2,570줄). 주소에 `?t=…` 가 붙어 있으면 점주 화면
  - 본사: 발주 · 가맹점 · 품목 · 정산 · 설정 (정산은 관리자만)
  - 점주: 발주하기 · 내 발주 내역 (핀 네 자리로 들어옴)
  - 25초마다 조용히 다시 받고, 실시간 알림이 오면 그때 바로. 시트가 열려 있으면 안 건드림
  - 스타일시트는 **Re:Bind 것을 통째로 물려받았습니다.** 두 앱이 같아 보여야 하니까요

### 화면으로 확인한 것 (로컬, 가짜 자료)

- 로그인 화면 — 서비스 고르기가 셋으로 늘고 Re:Store 가 기본 ✓
- 발주 목록 · 상태 칩 · 손이 가야 하는 것이 위로 ✓
- **금액 셈** — 면세와 과세를 갈라 세는 것까지 검산했습니다
  - 생닭 6×32,000=192,000(면세) + 튀김가루 4×41,000 + 박스 2×38,000=240,000(과세)
    → 부가세 24,000 → 합계 **456,000** ✓
  - 출고수량이 주문보다 적으면 그만큼만: 생닭 10개 주문 / 9개 출고 → 288,000 ✓
- 발주 상세 · 상태 걸음 · 출고수량 고치기 ✓
- 점주 화면 — 품목 담기, `+` 첫 클릭에 최소수량(2)까지 점프, 장바구니 합계 154,200 ✓
- 점주 내역 — 주문 10 / 출고 9 가 빨갛게 ✓
- 정산 집계 — 강남역점 283,200 · 수원영통점 453,000 · 합계 736,200 ✓
- **거래명세서 A4** — 공급자/공급받는자, 날짜별 품목표, 면세/과세 분리,
  한글 금액("일금 이십팔만삼천이백원정") ✓

### 설치 — 2026-08-30 · 거의 끝났습니다

| | 했나 | 무엇 |
|---|---|---|
| 표 만들기 | ✅ | 표 4개 · 접근규칙 16개 · storage 정책 4개 · 보관함 · 실시간 4개 |
| **권한(grant)** | ✅ | **빠뜨렸다가 나중에 붙였습니다.** 아래를 보세요 |
| 서버 함수 | ✅ | `store-gate` 배포 (`--no-verify-jwt`) |
| CORS | ✅ | 세 주소 함께 + 다섯 함수 재배포. Re:Bind·Re:Call 안 멈춘 것 확인 |
| 깃허브 | ✅ | MotoDNA/Restore (공개) · Pages 빌드 성공 |
| DNS | ✅ | `restore` → `motodna.github.io` (가비아) |
| 회사·관리자 | ✅ | `9DORO` · 구도로통닭 · admin (남덕현) |
| 품목 채우기 | ✅ | 12개 (지어낸 예시 단가) |
| HTTPS 인증서 | ✅ | Let's Encrypt 발급 완료 · https 강제 켜짐 |

`GUDORO`(잘못 만든 빈 회사)는 사장님 확인을 받고 지웠습니다. 남은 회사는
`9DORO`(구도로통닭) · `ACTIVA` · `BKT` 셋입니다.

### 실제로 왕복 시험한 것 (시험점 T-001, 핀 4821)

문지기를 셸에서 직접 두들겼습니다. 앱 화면이 아니라 **서버가 진짜로 막는지**를 봤습니다.

| 시험 | 결과 |
|---|---|
| 핀 없이 | `핀을 넣어 주세요` + needPin ✓ |
| 틀린 핀 | `핀이 다릅니다` ✓ (없는 토큰과 같은 말로 돌려주어 점포 존재 여부를 안 알려 줌) |
| 맞는 핀 | 본사·점포·품목 12개 정상 ✓ |
| 최소수량(2) 무시하고 1개 | `생닭 10호은(는) 최소 2개부터…` 로 거부 ✓ |
| **단가를 1원으로 조작** | **무시됨.** 서버가 32,000원·38,000원을 직접 붙임 ✓ |
| **면세를 과세로 조작** | **무시됨.** 생닭은 면세로 유지 ✓ |
| 금액 셈 | 면세 96,000 + 과세 76,000 + 부가세 7,600 = **179,600원** ✓ |


### 인증서가 안 나왔던 이유 — 다음에 또 겪습니다

깃허브 페이지에 **도메인을 DNS 보다 먼저 등록**하면, 깃허브가 그때 DNS 확인에
실패하고 **다시 시도하지 않습니다.** 한 시간을 기다려도 안 나옵니다.
DNS·CAA 는 아무 문제가 없었습니다.

고치는 법 — 도메인을 **뗐다가 다시 붙입니다.** 그러면 검사를 다시 돌리고
바로 발급됩니다.

```bash
echo '{"cname":null}' | gh api -X PUT repos/MotoDNA/Restore/pages --input -
# 빌드가 끝나길 기다렸다가
gh api -X PUT repos/MotoDNA/Restore/pages -f cname="restore.dnalabs.kr"
# 인증서가 approved 가 되면
gh api -X PUT repos/MotoDNA/Restore/pages -F https_enforced=true
```

떼는 동안 깃허브가 저장소의 `CNAME` 파일을 지웠다가 다시 만듭니다
(커밋 두 개가 자동으로 생깁니다). 정상입니다.

### ⚠ ALLOWED_ORIGIN 이 한 번 저절로 되돌아갔습니다

설치 도중 세 주소로 넣고 확인까지 했는데, 얼마 뒤 **예전 값(rebind·recall 둘)으로
되돌아가** 있었습니다. `.env` 탓은 아니었고 원인을 못 찾았습니다.
다시 넣고 함수를 재배포해 지금은 셋 다 통과합니다.

**Re:Store 가 갑자기 "인터넷이 끊긴 것 같습니다" 를 보이면 이것부터 의심하세요.**

```bash
curl -s -i -X OPTIONS https://izrtclsqhsgkuwsffifn.supabase.co/functions/v1/store-gate \
  -H "Origin: https://restore.dnalabs.kr" -H "Access-Control-Request-Method: POST" \
  | grep -i access-control-allow-origin
```
돌아온 주소가 `restore.dnalabs.kr` 이 아니면 되돌아간 것입니다.

### 실제 주소에서 왕복 확인 (2026-08-30)

`https://restore.dnalabs.kr/?t=…` 를 브라우저로 열어 핀 4821 을 넣고 확인했습니다.

- 핀 화면 → 통과 → **시험점(강남역점) · 구도로통닭 · 김철수 점주님** ✓
- 품목 12개, 분류 칩 5개(전체12·식자재6·소스3·포장재2·비품1) ✓
- 내 발주 내역 → `20260830-001 접수` · 합계 **179,600원** · 걸음 접수✓ ✓

### ⚠ 설치 중에 걸린 두 가지

**1) 순서를 틀렸습니다 — 회사를 SQL 로 먼저 만들었습니다.**
Re:Call 의 `bootstrap` 은 이미 있는 회사 코드를 거부합니다. 회사와 계정을 함께
만들기 때문입니다. 그래서 `0002_gudoro.sql` 을 `0002_company.sql` 로 바꿔,
회사를 만들지 않고 **이미 있는 회사**에 `apps` 를 열고 품목만 채우게 했습니다.

**2) 권한(grant)을 빠뜨렸습니다 — 증상이 엉뚱했습니다.**
이 프로젝트는 Supabase 기본 권한에 기대지 않고 표마다 `authenticated` 와
`service_role` 에게 일일이 권한을 줍니다(0001_init·0003_works 가 그렇게 합니다).
그걸 빠뜨리니 **표도 있고 규칙도 맞는데** 점주 링크가 계속 `없는 주소입니다` 만
돌려줬습니다. 토큰이 틀린 줄 알고 한참 봤습니다 — 문지기가 점포를 못 찾은 것이었습니다.
`sql/0003_grants.sql` 로 붙였고, `0001_restore.sql` 에도 넣어 두어 다음 설치부터는
처음부터 붙습니다.

> 기억할 것: **권한은 바깥문, RLS 는 안쪽문**입니다.
> 바깥문이 잠겨 있으면 안쪽 규칙을 볼 기회조차 없습니다.

---

## ⚠ 알고 있는 구멍

### 1. 직원에게 금액은 "화면에서만" 가려집니다

지금은 화면이 안 그릴 뿐이고, **데이터베이스는 직원에게도 단가를 내보냅니다.**
개발자 도구를 열면 보입니다.

Re:Bind 도 처음에 똑같았고, 나중에 금액을 딴 표(`project_money`)로 빼고
창(`project_money_v`)으로 가려서 진짜로 막았습니다. 여기도 같은 일을 해야 합니다.
급하지 않은 이유는 지금 쓰는 회사에 직원 계정이 없기 때문입니다 —
**직원 계정을 만들기 전에** 먼저 하세요.

### 2. 핀이 데이터베이스에 그대로 들어 있습니다

암호로 바꿔 두지 않았습니다. 본사가 "이 점포 핀이 뭐였지" 를 볼 수 있어야 해서입니다.
그 대신 틀린 횟수를 세서 늦춥니다. 이 정도가 맞다고 봤지만, 점포가 수백 개로 늘면
다시 생각해야 합니다.

### 3. 세 앱이 `ALLOWED_ORIGIN` 값 하나를 함께 씁니다

Re:Store 주소만 적어 넣으면 그 순간 Re:Bind 와 Re:Call 이 멈춥니다.
**늘 세 주소를 함께** 적고, **다섯 함수를 모두 다시 배포**해야 합니다.

### 4. 다른 두 앱의 로그인 화면에는 아직 Re:Store 가 없습니다

`store.html` 의 서비스 고르기는 셋이지만, `bindery.html` 과 `network-dna.html` 은
아직 둘입니다. 그쪽에서 Re:Store 로 건너갈 수가 없습니다.

---

## 다음에 할 일

- [ ] 설치 (`설치방법.md` 1~5번)
- [ ] **구도로 통닭 실제 품목·단가 받기** — 면세/과세 구분이 특히 중요합니다
- [ ] 실제 링크로 왕복 확인 — 점주 링크를 시크릿 창에서 열어 발주까지
- [ ] 금액을 딴 표로 빼기 (위 구멍 1). **직원 계정을 만들기 전에**
- [ ] `bindery.html` · `network-dna.html` 로그인 화면에 Re:Store 칸 넣기 (위 구멍 4)
- [ ] 발주 마감시간 — 외식은 "몇 시까지 넣은 것은 내일 출고" 가 있습니다. 지금은 없습니다
- [ ] 점포 등급별 단가 — 같은 품목을 점포마다 다르게 주는 곳이 있습니다. 지금은 한 값뿐
- [ ] 로열티 정산 — 매출의 몇 % 를 따로 청구하는 구조. 지금은 물품 대금만
